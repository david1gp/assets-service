/**
 * Text color for inline error copy on the page background. `red-800` on white is
 * 7.4:1 and `red-300` on `slate-900` is 8.1:1, both clear of WCAG AA.
 */
export const uiErrorTextClassesRead = (): string => "text-red-800 dark:text-red-300"
