import type { ProjectSourceConfiguration } from "./projectSourceConfigurationSchema.js"

export const projectSourceConfigurationDefaults: ProjectSourceConfiguration = {
  image: "images",
  video: "videos",
  document: "documents",
  font: "fonts",
}
